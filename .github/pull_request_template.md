## Summary

- What changed?
- Why was it needed?

## Validation

- [ ] `python -m unittest discover -s tests -p "test_*.py"`
- [ ] Manual validation done (if AutoCAD-dependent)

## Checklist

- [ ] Docs updated where needed (`README.md`, `docs/*`)
- [ ] No local artifacts committed (`logs/`, `out/`, `tmp/`, `.venv/`)
- [ ] Backward compatibility impact considered
